# 🚀 一键快速部署（5分钟完成）

## 📋 部署流程概览

```
GitHub仓库 → Vercel(前端) + Railway(后端) → 用户访问网址
```

**用户使用方式：通过浏览器访问网址，无需下载安装**

---

## ⚡ 快速开始（3个步骤）

### 步骤1：准备GitHub仓库（2分钟）

```bash
cd F:\ProReport-GenAI

# 如果还没有Git仓库
git init
git add .
git commit -m "Ready for deployment"

# 在GitHub创建仓库后，执行：
git remote add origin https://github.com/your-username/proreport-genai.git
git branch -M main
git push -u origin main
```

### 步骤2：部署前端到Vercel（2分钟）

1. 访问：https://vercel.com
2. 用GitHub登录
3. 点击 "New Project" → 选择你的仓库
4. 配置：
   - Build Command: `npm run build:client`
   - Output Directory: `dist`
5. 点击 "Deploy"
6. **复制部署后的URL**（例如：`https://xxx.vercel.app`）

### 步骤3：部署后端到Railway（3分钟）

1. 访问：https://railway.app
2. 用GitHub登录
3. 点击 "New Project" → "Deploy from GitHub repo"
4. 选择你的仓库
5. 设置：
   - Root Directory: `server`
   - 环境变量：
     ```
     GEMINI_API_KEY=你的API密钥
     JWT_SECRET=随机字符串（32字符以上）
     FRONTEND_URL=步骤2获取的Vercel URL
     NODE_ENV=production
     DATA_PATH=./data
     ```
6. 等待部署完成
7. 生成公共域名，**复制URL**（例如：`https://xxx.up.railway.app`）

### 步骤4：连接前后端（1分钟）

回到Vercel：
1. Settings → Environment Variables
2. 添加：`VITE_API_URL` = `https://xxx.up.railway.app/api`
3. 重新部署

---

## ✅ 完成！

用户访问：`https://your-app.vercel.app`

**无需下载、无需安装、打开浏览器即可使用！**

---

## 📝 详细步骤

查看 `快速部署指南.md` 获取完整的图文说明。

---

## 🎯 部署检查清单

使用 `DEPLOY_CHECKLIST.md` 确保所有步骤都完成。

---

## 💡 提示

- **免费额度：** Vercel和Railway都有充足的免费额度
- **自动更新：** 推送代码到GitHub后自动重新部署
- **监控：** 两个平台都提供监控和日志功能

