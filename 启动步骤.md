# 🚀 详细启动步骤

## 方法一：一键启动（最简单，推荐）

### 步骤 1：打开终端
在 Cursor 中打开终端，或使用 PowerShell/CMD

### 步骤 2：进入项目目录
```bash
cd F:\ProReport-GenAI
```

### 步骤 3：启动项目
```bash
npm run dev
```

这个命令会**同时启动前端和后端**，你会看到两个服务器的输出。

### 步骤 4：访问应用
打开浏览器访问：**http://localhost:3000**

---

## 方法二：分别启动（适合调试）

### 启动后端

**步骤 1：打开第一个终端**
```bash
cd F:\ProReport-GenAI\server
npm run dev
```

等待看到：
```
🚀 Server running on http://localhost:3001
📝 Environment: development
```

**步骤 2：验证后端运行**
打开浏览器访问：http://localhost:3001/api/health

应该看到：`{"success":true,"message":"Server is running"}`

### 启动前端

**步骤 3：打开第二个终端**
```bash
cd F:\ProReport-GenAI
npm run dev:client
```

等待看到：
```
  VITE v6.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

**步骤 4：访问应用**
打开浏览器访问：**http://localhost:3000**

---

## 📋 启动前检查清单

### ✅ 1. 检查后端环境变量
确保 `F:\ProReport-GenAI\server\.env` 文件存在并包含：

```env
PORT=3001
NODE_ENV=development
GEMINI_API_KEY=AIzaSyD7L7GzfFwFu0BVA5N0EEOKCIe20mlAeK8
JWT_SECRET=proreport-genai-secret-key-change-in-production
DATA_PATH=./data
FRONTEND_URL=http://localhost:3000
```

### ✅ 2. 检查依赖是否安装

**检查后端依赖：**
```bash
cd F:\ProReport-GenAI\server
Test-Path node_modules
```
应该返回 `True`

**检查前端依赖：**
```bash
cd F:\ProReport-GenAI
Test-Path node_modules
```
应该返回 `True`

如果返回 `False`，需要安装：
```bash
# 安装后端依赖
cd F:\ProReport-GenAI\server
npm install

# 安装前端依赖
cd F:\ProReport-GenAI
npm install
```

### ✅ 3. 检查端口是否被占用

**检查 3000 端口（前端）：**
```bash
netstat -ano | findstr ":3000"
```

**检查 3001 端口（后端）：**
```bash
netstat -ano | findstr ":3001"
```

如果端口被占用，可以：
- 关闭占用端口的程序
- 或修改端口号（在 `.env` 和 `vite.config.ts` 中）

---

## 🎯 启动后的验证

### 1. 后端健康检查
访问：http://localhost:3001/api/health

**预期结果：**
```json
{
  "success": true,
  "message": "Server is running"
}
```

### 2. 前端页面
访问：http://localhost:3000

**预期结果：**
- 看到封面页（Cover Page）
- 点击按钮可以进入登录页

### 3. 测试登录
- 注册一个新用户
- 登录后应该能看到 dashboard

---

## ⚠️ 常见问题

### 问题 1：`npm run dev` 报错 "concurrently 不是内部或外部命令"

**解决方法：**
```bash
npm install concurrently --save-dev
```

### 问题 2：后端启动失败，提示 "GEMINI_API_KEY is not configured"

**解决方法：**
检查 `server/.env` 文件是否存在，确保包含 `GEMINI_API_KEY`

### 问题 3：前端无法连接后端，出现 CORS 错误

**解决方法：**
- 确保后端已启动
- 检查 `server/.env` 中的 `FRONTEND_URL` 配置
- 检查后端 CORS 配置

### 问题 4：端口被占用

**解决方法：**
```bash
# 查找占用端口的进程
netstat -ano | findstr ":3001"

# 结束进程（替换 PID 为实际进程ID）
taskkill /PID <PID> /F
```

或修改端口：
- 后端：修改 `server/.env` 中的 `PORT`
- 前端：修改 `vite.config.ts` 中的 `port`

---

## 📝 启动命令总结

| 命令 | 说明 |
|------|------|
| `npm run dev` | 同时启动前后端（推荐） |
| `npm run dev:client` | 仅启动前端 |
| `npm run dev:server` | 仅启动后端 |
| `npm run build` | 构建生产版本 |
| `npm start` | 启动生产服务器 |

---

## 🎉 成功启动的标志

### 后端成功：
```
🚀 Server running on http://localhost:3001
📝 Environment: development
```

### 前端成功：
```
  VITE v6.x.x  ready in xxx ms
  ➜  Local:   http://localhost:3000/
```

### 浏览器访问：
- http://localhost:3000 - 看到应用界面
- http://localhost:3001/api/health - 看到健康检查响应

---

## 💡 提示

1. **保持终端打开**：关闭终端会停止服务器
2. **查看日志**：终端会显示请求日志和错误信息
3. **热重载**：修改代码后会自动重新加载
4. **停止服务器**：在终端按 `Ctrl + C`






