# 项目位置说明

## 项目新位置

**项目路径**：`F:\ProReport-GenAI`

项目已成功从 `C:\Users\lh\Contacts` 移动到 F 盘，不再占用系统盘空间。

## 如何使用

### 1. 打开项目

在文件资源管理器中打开：
```
F:\ProReport-GenAI
```

或在命令行中：
```powershell
cd F:\ProReport-GenAI
```

### 2. 运行项目

```powershell
npm run dev
```

应用将在 `http://localhost:3000` 启动。

### 3. 在 Cursor 中打开项目

1. 打开 Cursor
2. 点击 `File` → `Open Folder`
3. 选择 `F:\ProReport-GenAI`
4. 开始编辑

## 重要提示

### 数据存储位置

- **用户数据**：仍然存储在浏览器的 localStorage 中（与项目位置无关）
- **项目文件**：现在在 F 盘
- **依赖包**：`node_modules` 文件夹在 F 盘（已重新安装）

### 旧位置的文件

原位置 `C:\Users\lh\Contacts` 的文件仍然存在，如果需要可以删除：
- 可以安全删除旧位置的 `node_modules` 文件夹（节省空间）
- 建议保留源代码文件作为备份，或确认新位置运行正常后再删除

### 环境变量

`.env.local` 文件已复制到新位置，包含你的 API Key，无需重新配置。

## 项目结构

```
F:\ProReport-GenAI\
├── App.tsx                 # 主应用组件
├── index.tsx              # 入口文件
├── index.html             # HTML 模板
├── package.json           # 项目配置
├── .env.local             # API Key 配置（已包含）
├── services/              # 服务文件
│   ├── authService.ts     # 用户认证服务
│   └── geminiService.ts   # Gemini API 服务
├── components/            # 组件文件
│   └── LoginView.tsx      # 登录界面
└── node_modules/          # 依赖包（已安装）
```

## 快捷方式（可选）

如果想快速访问项目，可以：

1. **创建桌面快捷方式**：
   - 右键 `F:\ProReport-GenAI` 文件夹
   - 选择"发送到" → "桌面快捷方式"

2. **固定到任务栏**：
   - 右键 Cursor 图标
   - 选择"固定到任务栏"
   - 设置工作目录为 `F:\ProReport-GenAI`

## 验证项目

运行以下命令验证项目是否正常：

```powershell
cd F:\ProReport-GenAI
npm run dev
```

如果看到 "Local: http://localhost:3000" 说明项目运行正常！






